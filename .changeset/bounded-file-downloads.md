---
---

Keep large browser downloads file-backed when the native save picker is
unavailable, verify deterministic payload integrity in CI, and recover OPFS
cleanup safely across reloads and concurrent tabs.
