---
"@peerbit/shared-fs": patch
---

Add an opt-in, generation-isolated Merkle v1 path-copy builder with bounded
patch and cache resources, sparse growth and truncation, fail-closed verified
source/sink adapters, prompt cancellation, and structural work counters.
