---
"@peerbit/please-lib": patch
---

Expose per-chunk timestamps and sources when exact manifest-entry blocks are confirmed in the receiver's local Peerbit store, so benchmarks can distinguish data availability, Peerbit persistence, and sink acceptance.
